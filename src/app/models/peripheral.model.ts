export class Peripheral {
  id?: any;
  uid?: number;         // a UID (number)
  vendor?: string;      // vendor (string)
  date_created?: Date;  //date created
  status?: boolean;     //status - online/offline
}
